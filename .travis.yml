# Configures builds for all branches and deployment on the "prod" branch.

language: node_js
node_js: node

install: npm ci
script: npm test && npm run build

deploy:
  provider: firebase
  edge: true
  skip-cleanup: true
  on:
    branch: master
